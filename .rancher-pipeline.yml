stages:
- name: build
  steps:
  - publishImageConfig:
      dockerfilePath: ./Dockerfile
      buildContext: .
      tag: blueforest/off-import:1.0.12
      pushRemote: true
      registry: index.docker.io
timeout: 60
